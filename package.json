{
  "name": "framecut-video-editor",
  "private": true,
  "version": "1.0.0",
  "description": "A focused Cloudflare-native browser video editor sponsored by Autter.",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "oxlint --deny-warnings",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "preview": "npm run build && vite preview",
    "seed:ffmpeg:local": "wrangler r2 object put autter-video-editor-media/runtime/ffmpeg-core-0.12.10.wasm --file node_modules/@ffmpeg/core/dist/esm/ffmpeg-core.wasm --content-type application/wasm --cache-control 'public, max-age=31536000, immutable' --local",
    "seed:ffmpeg": "wrangler r2 object put autter-video-editor-media/runtime/ffmpeg-core-0.12.10.wasm --file node_modules/@ffmpeg/core/dist/esm/ffmpeg-core.wasm --content-type application/wasm --cache-control 'public, max-age=31536000, immutable' --remote",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types",
    "check": "npm run cf-typegen && npm run lint && npm run test && npm run build",
    "deploy:dry": "npm run build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "@ffmpeg/core": "0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2",
    "@phosphor-icons/react": "^2.1.10",
    "better-auth": "^1.6.25",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.50.0",
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "oxlint": "^1.71.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.118.0"
  }
}
